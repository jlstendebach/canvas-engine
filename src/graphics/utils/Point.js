import { Vec2 } from "../../math/Vec2.js";

export class Point extends Vec2 {
    // This is a simple alias for Vec2, but it can be extended in the future to 
    // add point-specific functionality.
}
